{
  "name": "eu5-control-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "Safety-first EU5 state reader and UI-control MCP scaffold.",
  "type": "commonjs",
  "scripts": {
    "test": "node --test",
    "check": "node --check src/read/save-inventory.js && node --check src/control/action-gate.js"
  },
  "engines": {
    "node": ">=20"
  }
}
